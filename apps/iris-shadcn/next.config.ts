import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.factly.in', 'static.vecteezy.com'], // Add your external image domain here

  },
};

export default nextConfig;
